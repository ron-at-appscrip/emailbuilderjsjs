{
  "name": "@usewaypoint-monorepo",
  "version": "0.0.6",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "target": "ES2022",
  "files": [
    "dist"
  ],
  "workspaces": [
    "packages/*"
  ],

  "scripts": {
    "build": "echo 'Root build skipped - use package-specific builds'",
    "build:all": "npm run build:blocks && npm run build:core && npm run build:email-builder",
    "build:blocks": "npm run build --workspace=packages/block-address && npm run build --workspace=packages/block-avatar && npm run build --workspace=packages/block-button && npm run build --workspace=packages/block-columns-container && npm run build --workspace=packages/block-contact && npm run build --workspace=packages/block-container && npm run build --workspace=packages/block-coupon && npm run build --workspace=packages/block-divider && npm run build --workspace=packages/block-footer && npm run build --workspace=packages/block-header && npm run build --workspace=packages/block-heading && npm run build --workspace=packages/block-html && npm run build --workspace=packages/block-image && npm run build --workspace=packages/block-map && npm run build --workspace=packages/block-order-summary && npm run build --workspace=packages/block-product-card && npm run build --workspace=packages/block-promo-banner && npm run build --workspace=packages/block-social-links && npm run build --workspace=packages/block-spacer && npm run build --workspace=packages/block-text",
    "build:core": "npm run build --workspace=packages/document-core",
    "build:email-builder": "npm run build --workspace=packages/email-builder",
    "build:vercel": "npm run build:all && npm run build --workspace=packages/editor-sample",
    "test": "npx jest"
  },
  "author": "carlos@usewaypoint.com",
  "license": "MIT",
  "overrides": {
    "csstype": "3.1.3"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@testing-library/react": "^14.2.1",
    "@types/jest": "^29.5.12",
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@vitejs/plugin-react": "^4.2.1",
    "eslint": "^8.56.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-simple-import-sort": "^11.0.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "prettier": "^3.2.5",
    "react-test-renderer": "^18.2.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "tsup": "^8.0.2",
    "typescript": "^5.3.3",
    "vite": "^5.1.0",
    "zod": "^3.22.4"
  }
}
